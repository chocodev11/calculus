---
id: 'nguyen-ham'
title: 'Nguyên Hàm'
description: 'Cho f, tìm tất cả F sao cho F''(x) = f(x) — họ nguyên hàm sai khác nhau hằng số C.'
courseSlug: 'tich-phan'
chapterSlug: 'nguyen-ham-va-ung-dung'
order: 0
xp: 10
---

<Slide title="Slide 1">
  <Sandbox type="B" manifest={
    {
      "interactionType": "B",
      "meta": {
        "parameterLabel": "Hằng số C"
      },
      "parameter": {
        "min": -4,
        "max": 4,
        "initial": 0
      },
      "system": {
        "resolution": 200,
        "view": {
          "xMin": -3.5,
          "xMax": 3.5,
          "yMin": -6,
          "yMax": 14
        },
        "curves": [
          {
            "expr": "2*x",
            "color": "#3498db",
            "label": "f(x) = 2x",
            "style": "solid",
            "width": 2
          },
          {
            "expr": "x*x + p",
            "color": "#e67e22",
            "label": "F(x) = x² + C",
            "style": "solid",
            "width": 2
          }
        ]
      },
      "reflections": [
        {
          "id": "at-zero",
          "trigger": "state.currentValue > -0.3 && state.currentValue < 0.3",
          "text": "$F(x) = x^2$. Đạo hàm $F'(x) = 2x = f(x)$. Đây là một nguyên hàm đúng."
        },
        {
          "id": "at-positive",
          "trigger": "state.currentValue > 2.5 && state.currentValue < 3.5",
          "text": "$F(x) = x^2 + 3$. Dịch lên $3$ đơn vị nhưng $F'(x)$ vẫn bằng $2x$ — đạo hàm của hằng số bằng $0$."
        },
        {
          "id": "at-negative",
          "trigger": "state.currentValue < -2.5 && state.currentValue > -3.5",
          "text": "$F(x) = x^2 - 3$. Dịch xuống — nhưng độ dốc tại mỗi $x$ không đổi. Mọi $x^2 + C$ đều có đạo hàm $2x$."
        },
        {
          "id": "at-extreme",
          "trigger": "state.currentValue > 3.5 || state.currentValue < -3.5",
          "text": "Dù $C$ bất kỳ, $F'(x) = f(x)$. Nguyên hàm không duy nhất — mà là cả một **họ hàm** dịch đứng."
        }
      ],
      "prompt": "Kéo $C$ để dịch đường cam $F(x) = x^2 + C$ lên xuống. Đạo hàm $F'(x)$ có thay đổi khi $C$ thay đổi không?"
    }
  } />
</Slide>

<Slide title="Slide 2">
  ### Nguyên hàm là gì?
  
  Bạn vừa thấy: dù $C$ thay đổi, mọi đường $F(x) = x^2 + C$ đều có cùng đạo hàm $F'(x) = 2x$. Ta gọi $F$ là **nguyên hàm** (antiderivative) của $f$ nếu $F'(x) = f(x)$ trên mọi $x$ trong miền xác định.
  
  Từ FTC Phần II: $\int_a^b f(x)\,dx = F(b) - F(a)$. Muốn tính tích phân, cần tìm nguyên hàm $F$. Nhưng nguyên hàm nào? Hằng số $C$ triệt tiêu khi trừ, nên **mọi nguyên hàm đều cho cùng kết quả**.
  ```json
  {
    "id": "math-nguyen-ham",
    "type": "math",
    "content": {
      "latex": "\\int f(x)\\,dx = F(x) + C, \\quad \\text{trong đó } F'(x) = f(x)"
    }
  }
  ```
</Slide>

<Slide title="Slide 3">
  ### Công thức nguyên hàm cơ bản
  
  Từ các quy tắc đạo hàm đã học, ta "đọc ngược": nếu $\frac{d}{dx}[x^{n+1}/(n+1)] = x^n$ thì $\int x^n\,dx = \frac{x^{n+1}}{n+1} + C$ với $n \neq -1$. Tương tự: $\int \cos(x)\,dx = \sin(x) + C$ vì $(\sin x)' = \cos x$.
  
  Ký hiệu $\int f(x)\,dx$ gọi là **tích phân bất định** — nó cho cả họ hàm $F(x) + C$, không phải một số. Khác với tích phân xác định $\int_a^b$ cho một giá trị duy nhất.
</Slide>

<Slide title="Slide 4">
  <Quiz id="quiz-l9-q1" question="Trong tương tác, khi bạn thay đổi $C$, điều gì xảy ra với đạo hàm $F'(x)$ của đường cam?" explanation="Dịch đứng không thay đổi độ dốc. $\frac{d}{dx}(x^2 + C) = 2x$ với mọi $C$, vì đạo hàm của hằng số bằng $0$. Đó là lý do nguyên hàm không duy nhất.">
    <Option value="a">$F'(x)$ tăng khi $C$ tăng</Option>
    <Option value="b">$F'(x)$ giảm khi $C$ tăng</Option>
    <Option value="c">$F'(x)$ không đổi — luôn bằng $f(x) = 2x$</Option>
    <Option value="d">$F'(x)$ đổi dấu khi $C$ đổi dấu</Option>
  </Quiz>
</Slide>

<Slide title="Slide 5">
  <Quiz id="quiz-l9-q2" question="Tìm nguyên hàm tổng quát: $\int 3x^2\,dx = ?$" explanation="Đọc ngược quy tắc lũy thừa: $\frac{d}{dx}[x^3] = 3x^2$, nên $\int 3x^2\,dx = x^3 + C$. Đáp án $6x$ là đạo hàm của $3x^2$, không phải nguyên hàm. Đáp án $x^3$ thiếu $+C$.">
    <Option value="a">$x^3 + C$</Option>
    <Option value="b">$6x + C$</Option>
    <Option value="c">$x^3$</Option>
    <Option value="d">$3x^3 + C$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 6">
  <Quiz id="quiz-l9-q3" question="$F(x) = \sin(x) + 5$ có phải nguyên hàm của $f(x) = \cos(x)$ không?" explanation="Nguyên hàm chỉ yêu cầu $F'(x) = f(x)$. Ở đây $F'(x) = \cos(x) = f(x)$ trên toàn bộ miền. Giá trị $F(0)$ hay hằng số $+5$ không ảnh hưởng — bất kỳ $\sin(x) + C$ nào cũng là nguyên hàm đúng.">
    <Option value="a">Không, vì $F(0) \neq 0$</Option>
    <Option value="b">Không, vì nguyên hàm đúng là $\sin(x)$, không có $+5$</Option>
    <Option value="c">Có, vì $F'(x) = \cos(x) = f(x)$</Option>
    <Option value="d">Chỉ đúng khi $x > 0$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 7">
  <Quiz id="quiz-l9-q4" question="Tại sao tích phân bất định $\int f(x)\,dx$ không cho một số mà cho cả họ hàm?" explanation="Nếu $F'(x) = f(x)$ thì $(F+C)' = F' + 0 = f$ với mọi hằng số $C$. Vô số hàm có cùng đạo hàm — chúng chỉ khác nhau một dịch đứng. Tích phân bất định biểu diễn cả họ này bằng $F(x) + C$.">
    <Option value="a">Vì tích phân luôn có nhiều nghiệm</Option>
    <Option value="b">Vì bài toán "tìm $F$ sao cho $F' = f$" có vô số lời giải — dịch đứng bất kỳ $F + C$ đều thỏa</Option>
    <Option value="c">Vì ký hiệu $\int$ nghĩa là "tập hợp"</Option>
    <Option value="d">Vì không có cận tích phân nên kết quả không xác định</Option>
  </Quiz>
</Slide>

<Slide title="Slide 8">
  <Quiz id="quiz-l9-q5" question="Tìm $\int (4x^3 - 2x)\,dx$." explanation="$\int 4x^3\,dx = x^4$ (vì $(x^4)' = 4x^3$) và $\int 2x\,dx = x^2$ (vì $(x^2)' = 2x$). Tổng hợp: $x^4 - x^2 + C$. Đáp án $12x^2 - 2$ là đạo hàm, đi sai hướng.">
    <Option value="a">$12x^2 - 2 + C$</Option>
    <Option value="b">$x^4 - x^2 + C$</Option>
    <Option value="c">$x^4 - x^2$</Option>
    <Option value="d">$4x^4 - 2x^2 + C$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 9">
  <Quiz id="quiz-l9-q6" question="Dùng FTC II: $\int_0^2 2x\,dx = F(2) - F(0)$ với $F(x) = x^2 + C$. Nếu chọn $C = 100$, kết quả thay đổi thế nào?" explanation="$F(2) - F(0) = (4 + C) - (0 + C) = 4$ với mọi $C$. Hằng số triệt tiêu khi trừ — đó là lý do tích phân xác định cho kết quả duy nhất dù nguyên hàm không duy nhất.">
    <Option value="a">Kết quả tăng thêm $100$</Option>
    <Option value="b">Kết quả tăng thêm $200$</Option>
    <Option value="c">Kết quả không đổi — hằng số triệt tiêu: $(4+100)-(0+100) = 4$</Option>
    <Option value="d">Không được chọn $C = 100$, phải chọn $C = 0$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 10">
  <Quiz id="quiz-l9-q7" question="Nếu $F(x)$ và $G(x)$ đều là nguyên hàm của $f(x)$ trên $\mathbb{R}$, thì $F(x) - G(x)$ bằng gì?" explanation="$(F - G)' = F' - G' = f - f = 0$. Hàm có đạo hàm bằng $0$ trên mọi nơi phải là hằng số (hệ quả của Định lý Giá trị Trung bình). Vậy $F(x) = G(x) + C$ — hai nguyên hàm bất kỳ chỉ khác nhau một hằng số.">
    <Option value="a">$0$ (chúng bằng nhau)</Option>
    <Option value="b">Một hằng số $C$</Option>
    <Option value="c">Một hàm tuyến tính $ax + b$</Option>
    <Option value="d">Không xác định được</Option>
  </Quiz>
</Slide>

<Slide title="Slide 11">
  <Quiz id="quiz-l9-q8" question="Gia tốc của vật $a(t) = 6t$ (m/s²). Biết vận tốc ban đầu $v(0) = 2$ m/s. Tìm $v(t)$." explanation="$v(t) = \int 6t\,dt = 3t^2 + C$. Dùng điều kiện đầu: $v(0) = 0 + C = 2$, suy ra $C = 2$. Vậy $v(t) = 3t^2 + 2$. Điều kiện đầu chính là cách chọn $C$ phù hợp trong họ nguyên hàm.">
    <Option value="a">$v(t) = 3t^2$</Option>
    <Option value="b">$v(t) = 3t^2 + 2$</Option>
    <Option value="c">$v(t) = 6t + 2$</Option>
    <Option value="d">$v(t) = 6t^2 + 2$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 12">
  <Quiz id="quiz-l9-q9" question="Tìm $\int \cos(x)\,dx$." explanation="$(\sin x)' = \cos x$, nên $\int \cos(x)\,dx = \sin(x) + C$. Đọc ngược quy tắc đạo hàm lượng giác: đạo hàm $\sin$ cho $\cos$, nên nguyên hàm $\cos$ cho $\sin$.">
    <Option value="a">$-\sin(x) + C$</Option>
    <Option value="b">$\sin(x) + C$</Option>
    <Option value="c">$\cos(x) + C$</Option>
    <Option value="d">$-\cos(x) + C$</Option>
  </Quiz>
</Slide>
