---
id: 'dao-ham-luong-giac'
title: 'Đạo hàm hàm lượng giác'
description: 'Đạo hàm của $\sin(x)$ là $\cos(x)$ — hai hàm là cùng một sóng, lệch pha một phần tư chu kỳ.'
courseSlug: 'dao-ham'
chapterSlug: 'ung-dung-dao-ham'
order: 0
xp: 15
---

<Slide title="Slide 1">
  <Sandbox type="B" manifest={
    {
      "interactionType": "B",
      "meta": {
        "parameterLabel": "Độ lệch pha $p$"
      },
      "parameter": {
        "min": 0,
        "max": 1.57,
        "initial": 0
      },
      "system": {
        "resolution": 400,
        "view": {
          "xMin": -6.3,
          "xMax": 6.3,
          "yMin": -1.5,
          "yMax": 1.5
        },
        "curves": [
          {
            "expr": "sin(x)",
            "color": "#3498db",
            "label": "f(x) = sin(x)",
            "style": "solid",
            "width": 2
          },
          {
            "expr": "cos(x + p)",
            "color": "#e74c3c",
            "label": "Đạo hàm thử: cos(x + p)",
            "style": "dashed",
            "width": 2
          }
        ]
      },
      "reflections": [
        {
          "id": "aligned",
          "trigger": "state.currentValue < 0.05",
          "text": "Tại $p = 0$: hai đường thẳng hàng hoàn hảo. Tại mọi đỉnh của $\\sin$, $\\cos$ đúng bằng $0$ — đạo hàm tại đỉnh bằng $0$."
        },
        {
          "id": "misaligned",
          "trigger": "state.currentValue > 0.3",
          "text": "Dịch pha làm mất sự thẳng hàng — $p = 0$ là giá trị đúng duy nhất. Đạo hàm của $\\sin$ chính xác là $\\cos$, không lệch."
        }
      ],
      "prompt": "Kéo độ lệch pha về $0$. Quan sát: tại các đỉnh và đáy của $\\sin$ (đường xanh), giá trị của đường đỏ là bao nhiêu? Điều đó có nghĩa gì về độ dốc của $\\sin$ tại những điểm đó?"
    }
  } />
</Slide>

<Slide title="Slide 2">
  ### Chứng minh: $\sin$ và $\cos$ liên kết qua đạo hàm
  
  Bạn đã xác minh sự thẳng hàng bằng quan sát. Đây là lý do nó chính xác: khai triển $\sin(x+h) = \sin x \cos h + \cos x \sin h$ rồi lấy giới hạn.
  
  Kết quả: $(\sin x)' = \sin x \cdot 0 + \cos x \cdot 1 = \cos x$. Dấu cộng và giá trị $1$ đến từ $\lim_{h\to 0}\frac{\sin h}{h} = 1$ và $\lim_{h\to 0}\frac{1-\cos h}{h} = 0$.
  ```json
  {
    "id": "math-m3l9-sin",
    "type": "math",
    "content": {
      "latex": "(\\sin x)' = \\cos x \\qquad (\\cos x)' = -\\sin x"
    }
  }
  ```
</Slide>

<Slide title="Slide 3">
  ### Đạo hàm $\tan(x)$ và kết hợp với quy tắc dây chuyền
  
  $(\tan x)' = \left(\frac{\sin x}{\cos x}\right)' = \frac{\cos^2 x + \sin^2 x}{\cos^2 x} = \frac{1}{\cos^2 x}$. Kết quả trực tiếp từ quy tắc thương và đồng nhất thức lượng giác $\sin^2 x + \cos^2 x = 1$.
  
  Kết hợp với quy tắc dây chuyền: $\frac{d}{dx}\sin(x^2) = \cos(x^2) \cdot 2x$. Mọi đạo hàm lượng giác kéo dài từ hai đạo hàm gốc $\sin$ và $\cos$.
  ```json
  {
    "id": "math-m3l9-tan",
    "type": "math",
    "content": {
      "latex": "(\\tan x)' = \\frac{1}{\\cos^2 x}"
    }
  }
  ```
</Slide>

<Slide title="Slide 4">
  <Quiz id="quiz-m3l9-q1" question="Tại các đỉnh của $f(x) = \sin(x)$, đường màu đỏ (đạo hàm thử tại $p = 0$) có giá trị bao nhiêu?" explanation="Tại cực đại cục bộ của $\sin$, tiếp tuyến nằm ngang: $f'(x) = \cos(x) = 0$ tại những điểm đó. Đạo hàm mã hóa tốc độ thay đổi, không phải vị trí.">
    <Option value="a">$1$ — khớp với chiều cao đỉnh.</Option>
    <Option value="b">$0$ — đạo hàm bằng $0$ tại đỉnh.</Option>
    <Option value="c">$-1$ — đối của đỉnh.</Option>
    <Option value="d">Dao động không theo quy luật.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 5">
  <Quiz id="quiz-m3l9-q2" question="Tính đạo hàm $f(x) = \sin(3x)$." explanation="Quy tắc dây chuyền: ngoài $\sin$, trong $3x$. $f'(x) = \cos(3x) \cdot 3 = 3\cos(3x)$. Nhân tử $3$ đến từ đạo hàm hàm trong.">
    <Option value="a">$\cos(3x)$</Option>
    <Option value="b">$3\cos(3x)$</Option>
    <Option value="c">$-\cos(3x)$</Option>
    <Option value="d">$3\sin(3x)$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 6">
  <Quiz id="quiz-m3l9-q3" question="Tại sao $(\sin x)' = \cos x$ (dương) chứ không phải $-\cos x$?" explanation="Dấu của $(\sin x)' = \cos x$ đến từ hai giới hạn nền tảng. Cả hai đều cho giá trị không âm ($+1$ và $0$), dẫn đến kết quả $+\cos x$.">
    <Option value="a">Vì sine được định nghĩa là hàm lượng giác dương.</Option>
    <Option value="b">Vì $\lim_{h\to 0} \frac{\sin h}{h} = 1$ (dương), không phải $-1$, xác định dấu trong phép dẫn xuất.</Option>
    <Option value="c">Theo quy ước để chu kỳ đạo hàm đẹp hơn.</Option>
    <Option value="d">Vì $\cos x \geq 0$ với mọi $x$.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 7">
  <Quiz id="quiz-m3l9-q4" question="Tính đạo hàm $f(x) = x^2 \cos(x)$." explanation="Quy tắc tích: $f'(x) = 2x\cos(x) + x^2(-\sin x) = 2x\cos(x) - x^2\sin(x)$. Tùy chọn D đúng về mặt đại số nhưng chưa rút gọn — B là dạng chuẩn.">
    <Option value="a">$2x\cos(x) + x^2\sin(x)$</Option>
    <Option value="b">$2x\cos(x) - x^2\sin(x)$</Option>
    <Option value="c">$-2x\sin(x)$</Option>
    <Option value="d">$x^2(-\sin x) + 2x\cos x$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 8">
  <Quiz id="quiz-m3l9-q5" question="Nếu lệch pha $p = \pi/2$, đường ứng viên là $\cos(x + \pi/2) = -\sin(x)$. Đây là đạo hàm của hàm nào?" explanation="$(\cos x)' = -\sin x$. Do đó $-\sin(x)$ là đạo hàm của $\cos(x)$, không phải $\sin(x)$. Lệch pha $\pi/2$ chuyển từ $\sin$ sang $\cos$.">
    <Option value="a">$\sin(x)$</Option>
    <Option value="b">$-\cos(x)$</Option>
    <Option value="c">$\cos(x)$</Option>
    <Option value="d">$\tan(x)$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 9">
  <Quiz id="quiz-m3l9-q6" question="Tính đạo hàm $h(x) = \tan(x^2)$." explanation="$h'(x) = \frac{1}{\cos^2(x^2)} \cdot 2x$. Tùy chọn B và C viết cùng một biểu thức theo hai cách khác nhau — cả hai đều đúng.">
    <Option value="a">$\frac{1}{\cos^2(x^2)}$</Option>
    <Option value="b">$\frac{2x}{\cos^2(x^2)}$</Option>
    <Option value="c">$2x \cdot \frac{1}{\cos^2(x^2)}$</Option>
    <Option value="d">B và C tương đương nhau.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 10">
  <Quiz id="quiz-m3l9-q7" question="Chu kỳ đạo hàm $\sin \to \cos \to -\sin \to -\cos \to \sin \to \ldots$ có chu kỳ $4$. Đạo hàm bậc bốn của $\sin(x)$ bằng gì?" explanation="Bốn bước trong chu kỳ: $\sin \to \cos \to -\sin \to -\cos \to \sin$. Sau bốn lần lấy đạo hàm, ta trở về $\sin(x)$.">
    <Option value="a">$-\sin(x)$</Option>
    <Option value="b">$\cos(x)$</Option>
    <Option value="c">$\sin(x)$</Option>
    <Option value="d">$-\cos(x)$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 11">
  <Quiz id="quiz-m3l9-q8" question="Độ dịch chuyển của dây đàn dao động là $s(t) = A\sin(\omega t)$. Vận tốc tức thời là:" explanation="Quy tắc dây chuyền: $s'(t) = A\cos(\omega t) \cdot \omega = A\omega\cos(\omega t)$. Tần số $\omega$ xuất hiện là đạo hàm hàm trong.">
    <Option value="a">$A\omega\cos(\omega t)$</Option>
    <Option value="b">$A\cos(\omega t)$</Option>
    <Option value="c">$-A\omega\sin(\omega t)$</Option>
    <Option value="d">$A\sin(\omega t)$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 12">
  <Quiz id="quiz-m3l9-q9" question="Tại $x = \pi/2$, $\sin(x)$ đạt cực đại. Không tính toán, $(\sin x)'|_{x=\pi/2}$ phải bằng bao nhiêu và tại sao?" explanation="Tại cực đại trơn, tiếp tuyến nằm ngang và $f' = 0$. Thật vậy $\cos(\pi/2) = 0$. Đạo hàm mã hóa tốc độ, không phải vị trí của hàm.">
    <Option value="a">$1$ — vì $\sin(\pi/2) = 1$.</Option>
    <Option value="b">$0$ — vì tại cực đại trơn, tiếp tuyến nằm ngang và tốc độ thay đổi tức thời bằng $0$.</Option>
    <Option value="c">$-1$ — vì hàm sắp giảm.</Option>
    <Option value="d">Không xác định — cực đại tạo ra góc nhọn.</Option>
  </Quiz>
</Slide>
