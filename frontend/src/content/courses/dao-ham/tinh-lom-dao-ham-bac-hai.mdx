---
id: 'tinh-lom-dao-ham-bac-hai'
title: 'Tính lõm và đạo hàm bậc hai'
description: 'Dấu của $f''''$ xác định tính lõm — một thuộc tính của hình dạng đường cong, không phụ thuộc vào vị trí thẳng đứng.'
courseSlug: 'dao-ham'
chapterSlug: 'ung-dung-dao-ham'
order: 0
xp: 15
---

<Slide title="Slide 1">
  <Sandbox type="B" manifest={
    {
      "interactionType": "B",
      "meta": {
        "parameterLabel": "Dịch đứng $p$"
      },
      "parameter": {
        "min": -5,
        "max": 5,
        "initial": 0
      },
      "system": {
        "resolution": 300,
        "view": {
          "xMin": -3,
          "xMax": 3,
          "yMin": -10,
          "yMax": 15
        },
        "curves": [
          {
            "expr": "pow(x, 4) - 6*x*x + p",
            "color": "#3498db",
            "label": "f(x)",
            "style": "solid",
            "width": 2
          },
          {
            "expr": "12*x*x - 12",
            "color": "#e74c3c",
            "label": "f″(x) = 12x² − 12",
            "style": "dashed",
            "width": 2
          }
        ]
      },
      "reflections": [
        {
          "id": "zero",
          "trigger": "state.currentValue > -0.5 && state.currentValue < 0.5",
          "text": "$f''$ tại $x = 0$: âm. Đường xanh lõm xuống quanh $x = 0$."
        },
        {
          "id": "shifting",
          "trigger": "state.currentValue > 2 || state.currentValue < -2",
          "text": "$f''$ không thay đổi. Tính lõm là thuộc tính của hình dạng, không phải vị trí thẳng đứng."
        }
      ],
      "prompt": "Kéo $p$ để dịch đường xanh lên xuống. Đường đỏ $f''$ có thay đổi không? Khi $f'' > 0$ (đường đỏ trên trục $x$), đường xanh cong theo hướng nào?"
    }
  } />
</Slide>

<Slide title="Slide 2">
  ### Tính lõm và đạo hàm bậc hai
  
  **$f''(x) > 0$** trên khoảng $I$: $f$ **lõm lên** trên $I$ — đường cong cong kiểu bát nhỏ, tiếp tuyến nằm dưới đường cong. **$f''(x) < 0$**: $f$ **lõm xuống** — tiếp tuyến nằm trên đường cong.
  
  **Điểm uốn:** Điểm mà $f''$ đổi dấu — đường cong chuyển giữa lõm lên và lõm xuống. Với $f(x) = x^4 - 6x^2$: $f''(x) = 12(x-1)(x+1)$. Điểm uốn tại $x = \pm 1$.
  ```json
  {
    "id": "math-m3l11-def",
    "type": "math",
    "content": {
      "latex": "f''(x) > 0 \\Rightarrow \\text{lõm lên} \\qquad f''(x) < 0 \\Rightarrow \\text{lõm xuống}"
    }
  }
  ```
</Slide>

<Slide title="Slide 3">
  ### Kiểm tra đạo hàm bậc hai cho cực trị
  
  **Kiểm tra đạo hàm bậc hai:** Nếu $f'(a) = 0$ và $f''(a) > 0$: cực tiểu cục bộ (đường cong lõm lên tại đó). Nếu $f''(a) < 0$: cực đại cục bộ. Nếu $f''(a) = 0$: không kết luận được — dùng kiểm tra đạo hàm bậc nhất.
  
  Ví dụ: $f(x) = x^3 - 3x$. Điểm tới hạn $x = \pm 1$. $f''(x) = 6x$. $f''(1) = 6 > 0$: cực tiểu. $f''(-1) = -6 < 0$: cực đại.
</Slide>

<Slide title="Slide 4">
  <Quiz id="quiz-m3l11-q1" question="Khi bạn kéo $p$ trong tương tác, đường đỏ $f''$ không thay đổi. Điều này hàm ý gì về tính lõm?" explanation="Dịch đứng ($+p$) cộng hằng số vào $f$ nhưng không ảnh hưởng $f'$ hay $f''$. Tính lõm là thuộc tính nội tại của hình dạng, không phải vị trí.">
    <Option value="a">Tính lõm phụ thuộc vào vị trí thẳng đứng của hàm.</Option>
    <Option value="b">Tính lõm là thuộc tính của hình dạng đường cong, không phụ thuộc vào dịch đứng.</Option>
    <Option value="c">$f''$ luôn là hàm hằng.</Option>
    <Option value="d">Điểm uốn dịch chuyển khi $p$ thay đổi.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 5">
  <Quiz id="quiz-m3l11-q2" question="Tìm điểm uốn của $f(x) = x^3 - 3x$." explanation="$f''(x) = 6x = 0$ tại $x = 0$. Kiểm tra đổi dấu: $f'' < 0$ khi $x < 0$, $f'' > 0$ khi $x > 0$ — dấu đổi, xác nhận điểm uốn tại $x = 0$.">
    <Option value="a">$x = \pm 1$</Option>
    <Option value="b">$x = 0$</Option>
    <Option value="c">$x = 1$</Option>
    <Option value="d">$x = -1$ và $x = 0$</Option>
  </Quiz>
</Slide>

<Slide title="Slide 6">
  <Quiz id="quiz-m3l11-q3" question="Ý nghĩa hình học của $f''(a) > 0$ là gì?" explanation="Lõm lên nghĩa là đường cong dạng bát — tiếp tuyến nằm dưới và ước lượng thấp $f$ trong lân cận điểm tiếp xúc.">
    <Option value="a">Hàm đang tăng tại $a$.</Option>
    <Option value="b">Tiếp tuyến tại $a$ nằm dưới đường cong trong lân cận của $a$ — lõm lên, đường cong cong theo hướng bát nhỏ.</Option>
    <Option value="c">Hàm đạt cực đại tại $a$.</Option>
    <Option value="d">Đạo hàm bậc hai là độ dốc tại $a$.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 7">
  <Quiz id="quiz-m3l11-q4" question="$f(x) = x^4 - 6x^2$. Tìm $f''(x)$ và xác định khoảng lõm lên." explanation="$f'(x) = 4x^3 - 12x$; $f''(x) = 12x^2 - 12 = 12(x^2-1) > 0$ khi $|x| > 1$.">
    <Option value="a">$f''(x) = 12x^2 - 12$; lõm lên trên $(-\infty,-1) \cup (1,\infty)$.</Option>
    <Option value="b">$f''(x) = 4x^3 - 12x$; lõm lên trên $(0,\infty)$.</Option>
    <Option value="c">$f''(x) = 12x^2 - 12$; lõm lên trên $(-1,1)$.</Option>
    <Option value="d">$f''(x) = 12x - 12$; lõm lên trên $(1,\infty)$.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 8">
  <Quiz id="quiz-m3l11-q5" question="Nếu $f(x) = x^3 - 3x^2 + p$ với $p$ thay đổi, điểm uốn thay đổi thế nào?" explanation="$f''(x) = 6x - 6 = 0$ tại $x = 1$, độc lập với $p$. Dịch đứng bảo toàn cấu trúc tính lõm.">
    <Option value="a">Nó dịch chuyển cùng $p$.</Option>
    <Option value="b">Nó cố định tại $x = 1$ bất kể $p$ — vì $f''$ không phụ thuộc $p$.</Option>
    <Option value="c">Nó biến mất với $p$ lớn.</Option>
    <Option value="d">Nó dịch đến $x = p$.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 9">
  <Quiz id="quiz-m3l11-q6" question="Dùng kiểm tra đạo hàm bậc hai để phân loại điểm tới hạn của $f(x) = x^4 - 4x^3$." explanation="$f'(x) = 4x^2(x-3)$. Điểm tới hạn: $x = 0$ và $x = 3$. $f''(x) = 12x^2 - 24x$. $f''(3) = 36 > 0$ → cực tiểu. $f''(0) = 0$ → không kết luận được từ kiểm tra bậc hai.">
    <Option value="a">Cực đại tại $x = 0$, cực tiểu tại $x = 3$.</Option>
    <Option value="b">Cực tiểu tại $x = 0$, cực đại tại $x = 3$.</Option>
    <Option value="c">Cực tiểu tại $x = 3$; $x = 0$ không kết luận được.</Option>
    <Option value="d">Cả hai đều là điểm uốn.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 10">
  <Quiz id="quiz-m3l11-q7" question="Hàm có thể có điểm uốn mà không có điểm tới hạn gần đó không?" explanation="Điểm uốn đòi hỏi $f''$ đổi dấu, không phải $f'$ bằng $0$. Hai điều kiện độc lập nhau về mặt logic.">
    <Option value="a">Không — điểm uốn và điểm tới hạn luôn trùng nhau.</Option>
    <Option value="b">Có — $f(x) = x^3 + x$ có điểm uốn tại $x = 0$ với $f'(0) = 1 \neq 0$ — hai khái niệm độc lập.</Option>
    <Option value="c">Không — $f''(a) = 0$ kéo theo $f'(a) = 0$.</Option>
    <Option value="d">Có, nhưng chỉ với hàm lượng giác.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 11">
  <Quiz id="quiz-m3l11-q8" question="Doanh thu $R(q)$ lõm lên với $q < 50$ và lõm xuống với $q > 50$. Điều này nói gì về doanh thu biên $R'(q)$?" explanation="Lõm lên → $R'' > 0$ → $R'$ tăng (mỗi đơn vị thêm đóng góp nhiều hơn đơn vị trước). Lõm xuống → $R'' < 0$ → $R'$ giảm (thu nhập giảm dần).">
    <Option value="a">$R'$ không đổi.</Option>
    <Option value="b">$R'$ tăng với $q < 50$ (doanh thu biên tăng) và giảm với $q > 50$ (thu nhập giảm dần).</Option>
    <Option value="c">$R'(50) = 0$.</Option>
    <Option value="d">$R'$ âm với $q > 50$.</Option>
  </Quiz>
</Slide>

<Slide title="Slide 12">
  <Quiz id="quiz-m3l11-q9" question="$f''(a) = 0$ — $a$ có nhất thiết là điểm uốn không?" explanation="$f(x) = x^4$: $f''(0) = 0$ nhưng $f''(x) = 12x^2 \geq 0$ luôn luôn — không đổi dấu — $x = 0$ không phải điểm uốn.">
    <Option value="a">Có — mỗi khi $f'' = 0$, hàm đổi tính lõm.</Option>
    <Option value="b">Không — $f''(a) = 0$ là điều kiện cần nhưng chưa đủ; dấu $f''$ phải thực sự đổi tại $a$.</Option>
    <Option value="c">Có — trừ khi $f'(a) \neq 0$.</Option>
    <Option value="d">Không — $f''(a) = 0$ nghĩa là $a$ là điểm tới hạn.</Option>
  </Quiz>
</Slide>
